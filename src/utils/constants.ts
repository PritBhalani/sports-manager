/** Default page size for list APIs and tables */
export const DEFAULT_PAGE_SIZE = 15;

/** Page size options for pagination controls */
export const PAGE_SIZE_OPTIONS = [10, 15, 25, 50] as const;

/** Breakpoint (px) for layout: sidebar stays open at or above this width (matches Tailwind md). */
export const LAYOUT_BREAKPOINT_MD = 768;
